# Requires Python 3.11 — set in Streamlit Cloud dashboard: Settings → Python version → 3.11
#
# Heart CV-gnal — pure Computer Vision framework

numpy>=1.26,<2.0
opencv-python>=4.10,<4.11
mediapipe==0.10.14
pillow>=10.4
flask>=3.0,<4.0

# Streamlit Cloud
streamlit>=1.33,<1.45
streamlit-webrtc==0.47.6
aiortc>=1.6
requests>=2.27

# Optional — VLM cross-validator (set ANTHROPIC_API_KEY to activate)
anthropic>=0.40,<1.0

# Test
pytest>=8.3,<8.4
